apiVersion: v1
kind: Pod
metadata:
  name: online-inference
  labels:
    app: online-inference
spec:
  containers:
    - name: online-inference
      image: vlamelni/online_inference:v3 # test1
      ports:
        - containerPort: 12000